define(['views/delivery_method_view'], function(DeliveryMethodView) {
    describe('Delivery Method View', function() {
        'use strict';

        var view = null, model = null;

        beforeEach(function() {

        });

        it('should define DeliveryMethodView type as function', function() {
            expect(typeof DeliveryMethodView).toEqual('function');
        });

        // it('should return new view', function() {
            // var templateRef = 'templates/unit/t_div';
            // var testDiv = '<div id="testTemplateContent">test content</div>';
//
            // view = new DeliveryMethodView({
                // el: '#testbed',
                // templates: templateRef,
                // initialize: function() {
                    // this.initTestProp = 'initTestVal';
                // }
            // });
            // // expect(view).not.toBe(null);
        // });
    });
});